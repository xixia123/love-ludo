"use client";

import { memo } from "react";
import {
  Mail,
  Lock,
  Eye,
  EyeOff,
  Shuffle,
  Users,
  LogIn,
  Layers,
  ChevronDown,
  Hash,
  Github,
} from "lucide-react";

export const MemoizedMail = memo(Mail);
export const MemoizedLock = memo(Lock);
export const MemoizedEye = memo(Eye);
export const MemoizedEyeOff = memo(EyeOff);
export const MemoizedShuffle = memo(Shuffle);
export const MemoizedUsers = memo(Users);
export const MemoizedLogIn = memo(LogIn);
export const MemoizedLayers = memo(Layers);
export const MemoizedChevronDown = memo(ChevronDown);
export const MemoizedHash = memo(Hash);
export const MemoizedGithub = memo(Github);
