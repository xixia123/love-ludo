"use client";

import { memo } from "react";
import { Mail, Lock, Eye, EyeOff, Shuffle } from "lucide-react";

export const MemoizedMail = memo(Mail);
export const MemoizedLock = memo(Lock);
export const MemoizedEye = memo(Eye);
export const MemoizedEyeOff = memo(EyeOff);
export const MemoizedShuffle = memo(Shuffle);
